// Dynamic style types
export type DynamicStyles = {
  fontSize: {
    amount: string;
    label: string;
    quickAmount: string;
    modalTitle: string;
    modalAmount: string;
    title: string;
  };
  spacing: {
    cardPadding: string;
    quickAmountGap: string;
    containerMargin: string;
  };
  size: {
    quickAmountMinWidth: string;
    tapCardImage: string;
    buttonSize: string;
  };
  numpad: {
    height: number;
    fontSize: number;
    margin: number;
  };
};
